package com.bitewise.repository; import com.bitewise.entity.*; import org.springframework.data.jpa.repository.JpaRepository; import java.time.LocalDate; import java.util.*; public interface DailySummaryRepository extends JpaRepository<DailySummary,UUID>{Optional<DailySummary> findByBusinessIdAndSummaryDate(UUID businessId,LocalDate date); List<DailySummary> findByBusinessIdAndSummaryDateBetweenOrderBySummaryDateAsc(UUID businessId,LocalDate from,LocalDate to);}
